#include <iostream>
#include <cmath>

using namespace std;

int main(){
	double a, b, c;
	cout << "bitte a eingeben: " << flush;
	cin >> a;
	cout << "bitte b eingeben: " << flush;
	cin >> b;
	cout << "bitte c eingeben: " << flush;
	cin >> c;

	double radiant = b * b- 4 * a * c;
	if(radiant < 0){
		cout << "keine reelen nullstellen" << flush;
	}else{
		double d = sqrt(radiant);
		double x0 = (-b + d) / (2 * a);
		double x1 = (-b - d) / (2 * a);
		if(x0 == x1){
			cout << "berührungspunkt bei " << x0 << flush;
		}else{
			cout << "x0: " << x0 << endl;
			cout << "x1: " << x1 << flush;
		}
	}
}
